name: Release Changed Plugins

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version for changed plugins (e.g., 1.0.0)'
        required: true
        type: string
      force_all:
        description: 'Force build all plugins (ignore changes)'
        required: false
        type: boolean
        default: false
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: write

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      changed-plugins: ${{ steps.changes.outputs.changed-plugins }}
      plugins-json: ${{ steps.changes.outputs.plugins-json }}
      version: ${{ steps.version.outputs.version }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history needed for change detection

      - name: Determine version
        id: version
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "version=${{ github.event.inputs.version }}" >> "$GITHUB_OUTPUT"
          else
            # For push/PR builds we don't create releases/tags, but we still need a version label.
            echo "version=dev-${{ github.run_number }}-${{ github.sha }}" >> "$GITHUB_OUTPUT"
          fi
      
      - name: Detect changed plugins (from settings.gradle.kts)
        id: changes
        shell: bash
        run: |
          set -euo pipefail
          # Discover plugin modules from settings.gradle.kts (same logic as release-plugins.yml)
          mapfile -t PLUGINS < <(
            sed -n 's/^[[:space:]]*include(\":\([^\"[:space:]]\+\)\".*$/\1/p' settings.gradle.kts \
              | sed 's/[[:space:]]//g' \
              | grep -v '^connectias-plugin-sdk$' \
              | sort -u
          )

          if [ ${#PLUGINS[@]} -eq 0 ]; then
            echo "❌ No plugin modules found in settings.gradle.kts"
            exit 1
          fi
          CHANGED_PLUGINS=()
          
          # If force_all is true, mark all as changed
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ "${{ github.event.inputs.force_all }}" == "true" ]; then
            echo "Force build all plugins enabled"
            CHANGED_PLUGINS=("${PLUGINS[@]}")
            echo "changed-plugins=$(IFS=','; echo "${CHANGED_PLUGINS[*]}")" >> $GITHUB_OUTPUT
            PLUGINS_JSON=$(python3 -c 'import json,sys; print(json.dumps(sys.argv[1:]))' "${CHANGED_PLUGINS[@]}")
            echo "plugins-json=${PLUGINS_JSON}" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          
          # Check changes for each plugin
          for plugin in "${PLUGINS[@]}"; do
            PLUGIN_DIR="${plugin}"
            
            # Find latest tag for this specific plugin
            # Tag format: v{version}-{plugin-name}
            LATEST_PLUGIN_TAG=$(git tag -l "v*-${plugin}" | sort -V | tail -1)
            
            if [ -z "$LATEST_PLUGIN_TAG" ]; then
              # No previous release for this plugin, mark as changed
              echo "No previous release for ${plugin}, marking as changed"
              echo "${plugin}=true" >> $GITHUB_OUTPUT
              CHANGED_PLUGINS+=("${plugin}")
            else
              # Check if plugin directory or root build files changed since last tag
              echo "Checking ${plugin} against tag ${LATEST_PLUGIN_TAG}"
              
              # Check for changes in:
              # - Plugin directory (specific changes)
              # - Root build.gradle.kts (affects all plugins)
              # - settings.gradle.kts (affects all plugins)
              # - gradle/ directory (affects all plugins)
              # - gradle.properties (affects all plugins)
              
              # First check if plugin-specific files changed
              PLUGIN_CHANGED=$(git diff --name-only ${LATEST_PLUGIN_TAG}..HEAD -- "${PLUGIN_DIR}/" || echo "")
              
              # Check if common build files changed (affect all plugins)
              COMMON_CHANGED=$(git diff --name-only ${LATEST_PLUGIN_TAG}..HEAD -- \
                "build.gradle.kts" \
                "settings.gradle.kts" \
                "gradle/" \
                "gradle.properties" || echo "")
              
              if [ -n "$PLUGIN_CHANGED" ]; then
                echo "Plugin-specific changes detected in ${plugin}:"
                echo "$PLUGIN_CHANGED"
                CHANGED_PLUGINS+=("${plugin}")
              elif [ -n "$COMMON_CHANGED" ]; then
                echo "Common build files changed, rebuilding ${plugin}:"
                echo "$COMMON_CHANGED"
                CHANGED_PLUGINS+=("${plugin}")
              else
                echo "No changes detected for ${plugin}"
              fi
            fi
          done
          
          if [ ${#CHANGED_PLUGINS[@]} -eq 0 ]; then
            echo "No plugins changed, nothing to build"
            echo "changed-plugins=" >> $GITHUB_OUTPUT
            echo "plugins-json=[]" >> $GITHUB_OUTPUT
          else
            echo "Changed plugins: ${CHANGED_PLUGINS[*]}"
            echo "changed-plugins=$(IFS=','; echo "${CHANGED_PLUGINS[*]}")" >> $GITHUB_OUTPUT
            PLUGINS_JSON=$(python3 -c 'import json,sys; print(json.dumps(sys.argv[1:]))' "${CHANGED_PLUGINS[@]}")
            echo "plugins-json=${PLUGINS_JSON}" >> "$GITHUB_OUTPUT"
          fi
      
      - name: Show changed plugins
        run: |
          echo "Changed plugins: ${{ steps.changes.outputs.changed-plugins }}"
          echo "Version label: ${{ steps.version.outputs.version }}"

  build-changed-plugins:
    needs: detect-changes
    if: needs.detect-changes.outputs.plugins-json != '[]'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        plugin: ${{ fromJSON(needs.detect-changes.outputs.plugins-json) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'gradle'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build ${{ matrix.plugin }} Release APK
        run: ./gradlew :${{ matrix.plugin }}:assembleRelease --no-daemon

      - name: Find APK file
        id: find-apk
        shell: bash
        run: |
          set -euo pipefail
          APK_PATH=$(find "${{ matrix.plugin }}/build/outputs/apk/release" -name "*.apk" -type f | head -1)
          if [ -z "$APK_PATH" ]; then
            echo "Error: APK file not found"
            exit 1
          fi
          echo "apk_path=$APK_PATH" >> $GITHUB_OUTPUT
          echo "apk_name=$(basename "$APK_PATH")" >> $GITHUB_OUTPUT
          echo "Found APK: $APK_PATH"

      - name: Generate SHA256 Checksum
        id: checksum
        run: |
          SHA256=$(sha256sum "${{ steps.find-apk.outputs.apk_path }}" | cut -d' ' -f1)
          echo "$SHA256  ${{ steps.find-apk.outputs.apk_name }}" > sha256sum.txt
          echo "sha256=$SHA256" >> $GITHUB_OUTPUT

      - name: Copy plugin manifest
        run: |
          if [ -f "${{ matrix.plugin }}/src/main/assets/plugin-manifest.json" ]; then
            cp "${{ matrix.plugin }}/src/main/assets/plugin-manifest.json" plugin-manifest.json
          else
            echo "{}" > plugin-manifest.json
          fi

      - name: Extract plugin ID from manifest
        id: plugin-id
        shell: bash
        run: |
          set -euo pipefail
          if command -v jq &> /dev/null && [ -f "plugin-manifest.json" ]; then
            PLUGIN_ID=$(jq -r '.pluginId // empty' plugin-manifest.json 2>/dev/null || echo "")
            if [ -z "$PLUGIN_ID" ] || [ "$PLUGIN_ID" == "null" ]; then
              PLUGIN_ID="${{ matrix.plugin }}"
            fi
          else
            PLUGIN_ID="${{ matrix.plugin }}"
          fi
          echo "plugin_id=$PLUGIN_ID" >> $GITHUB_OUTPUT

      - name: Create release tag name
        id: tag
        run: |
          TAG_NAME="v${{ needs.detect-changes.outputs.version }}-${{ matrix.plugin }}"
          echo "tag_name=$TAG_NAME" >> $GITHUB_OUTPUT

      - name: Create Git Tag
        if: github.event_name == 'workflow_dispatch'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          TAG_NAME="${{ steps.tag.outputs.tag_name }}"
          if git rev-parse "$TAG_NAME" >/dev/null 2>&1; then
            echo "Tag $TAG_NAME already exists, skipping tag creation"
          else
            git tag -a "$TAG_NAME" -m "Release ${{ matrix.plugin }} v${{ needs.detect-changes.outputs.version }}"
            git push origin "$TAG_NAME"
          fi

      - name: Create GitHub Release
        if: github.event_name == 'workflow_dispatch'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.tag_name }}
          name: ${{ matrix.plugin }} v${{ needs.detect-changes.outputs.version }}
          body: |
            # ${{ matrix.plugin }} v${{ needs.detect-changes.outputs.version }}

            ## Plugin Information
            - **Plugin ID:** ${{ steps.plugin-id.outputs.plugin_id }}
            - **Version:** ${{ needs.detect-changes.outputs.version }}

            ## Artifacts
            - APK: `${{ steps.find-apk.outputs.apk_name }}`
            - SHA256: `${{ steps.checksum.outputs.sha256 }}`
          files: |
            ${{ steps.find-apk.outputs.apk_path }}
            plugin-manifest.json
            sha256sum.txt
          draft: false
          prerelease: false

      - name: Upload build artifacts (push/PR)
        if: github.event_name != 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.plugin }}-${{ needs.detect-changes.outputs.version }}
          path: |
            ${{ steps.find-apk.outputs.apk_path }}
            plugin-manifest.json
            sha256sum.txt

  summary:
    needs: [detect-changes, build-changed-plugins]
    if: always() && needs.detect-changes.outputs.changed-plugins != ''
    runs-on: ubuntu-latest
    steps:
      - name: Release Summary
        run: |
          echo "## Release Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Version:** ${{ needs.detect-changes.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Changed Plugins:" >> $GITHUB_STEP_SUMMARY
          echo "${{ needs.detect-changes.outputs.changed-plugins }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Build Status:" >> $GITHUB_STEP_SUMMARY
          echo "- build-changed-plugins: ${{ needs.build-changed-plugins.result }}" >> $GITHUB_STEP_SUMMARY
